---
import { getCollection } from 'astro:content';
import { SITE, CONTACT, SOCIAL, mapsUrl } from '@/consts';

interface Props {
  title?: string;
  description?: string;
  image?: string;
  noindex?: boolean;
  type?: 'website' | 'article';
  publishedTime?: Date;
}

const {
  title,
  description = SITE.defaultDescription,
  image = '/og-default.jpg',
  noindex = false,
  type = 'website',
  publishedTime,
} = Astro.props;

const pageTitle = title ?? SITE.defaultTitle;
const canonical = new URL(Astro.url.pathname, SITE.url).href;
const ogImage = new URL(image, SITE.url).href;
const isHome = Astro.url.pathname === '/';

const servicios = (await getCollection('servicios')).sort((a, b) => a.data.order - b.data.order);

const localBusiness = {
  '@context': 'https://schema.org',
  '@type': 'ProfessionalService',
  '@id': `${SITE.url}/#business`,
  name: SITE.name,
  legalName: SITE.legalName,
  url: SITE.url,
  telephone: CONTACT.phoneIntl,
  email: CONTACT.email,
  foundingDate: String(SITE.foundingYear),
  description: SITE.defaultDescription,
  image: new URL('/og-default.jpg', SITE.url).href,
  logo: new URL('/logo-mipc.png', SITE.url).href,
  priceRange: '$$',
  currenciesAccepted: 'COP',
  address: {
    '@type': 'PostalAddress',
    streetAddress: CONTACT.street,
    addressLocality: CONTACT.city,
    addressRegion: CONTACT.region,
    addressCountry: CONTACT.country,
  },
  geo: { '@type': 'GeoCoordinates', latitude: CONTACT.lat, longitude: CONTACT.lng },
  hasMap: mapsUrl(),
  areaServed: { '@type': 'City', name: 'Medellín' },
  openingHours: CONTACT.hoursSchema,
  sameAs: [SOCIAL.facebook, SOCIAL.instagram, mapsUrl()],
  hasOfferCatalog: {
    '@type': 'OfferCatalog',
    name: 'Servicios de tecnología',
    itemListElement: servicios.map((s) => ({
      '@type': 'Offer',
      itemOffered: {
        '@type': 'Service',
        name: s.data.title,
        url: new URL(`/servicios/${s.id}/`, SITE.url).href,
      },
    })),
  },
};

const website = {
  '@context': 'https://schema.org',
  '@type': 'WebSite',
  url: SITE.url,
  name: SITE.name,
  inLanguage: SITE.lang,
};
---

<title>{pageTitle}</title>
<meta name="description" content={description} />
<link rel="canonical" href={canonical} />
{noindex && <meta name="robots" content="noindex, nofollow" />}

<meta property="og:type" content={type} />
<meta property="og:site_name" content={SITE.name} />
<meta property="og:locale" content="es_CO" />
<meta property="og:title" content={pageTitle} />
<meta property="og:description" content={description} />
<meta property="og:url" content={canonical} />
<meta property="og:image" content={ogImage} />
<meta property="og:image:width" content="1200" />
<meta property="og:image:height" content="630" />
{publishedTime && <meta property="article:published_time" content={publishedTime.toISOString()} />}

<meta name="twitter:card" content="summary_large_image" />
<meta name="twitter:title" content={pageTitle} />
<meta name="twitter:description" content={description} />
<meta name="twitter:image" content={ogImage} />

<meta name="geo.region" content="CO-ANT" />
<meta name="geo.placename" content="Medellín" />

<script is:inline type="application/ld+json" set:html={JSON.stringify(localBusiness)} />
{isHome && <script is:inline type="application/ld+json" set:html={JSON.stringify(website)} />}
